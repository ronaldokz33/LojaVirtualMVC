@model LojaVirtualMVC.WEB.Models.ProdutosViewModel
@using LojaVirtualMVC.WEB.HtmlHelpers

@foreach (var p in Model.Produtos)
{
   @Html.Partial("ProdutoDetalhe",p)
}

@Html.PageLinks(Model.Paginacao, x => Url.Action("ListaProdutos", new { pagina = x }))